{
  "name": "@serve-droid/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@serve-droid/core": "workspace:*",
    "@serve-droid/mcp": "workspace:*",
    "@serve-droid/server": "workspace:*",
    "commander": "^15.0.0"
  }
}
