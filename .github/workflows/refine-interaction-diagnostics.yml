name: Refine interaction diagnostics

on:
  push:
    branches:
      - feat/typed-interaction-errors
    paths:
      - .github/workflows/refine-interaction-diagnostics.yml

permissions:
  contents: write

env:
  PNPM_VERSION: 10.28.0

jobs:
  refine:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: npm install --global pnpm@${{ env.PNPM_VERSION }}
      - run: pnpm install --frozen-lockfile
      - run: python scripts/refine-interaction-diagnostics.py
      - name: Verify refined diagnostics
        run: |
          pnpm exec prettier --write \
            packages/core/src/interaction-errors.ts \
            packages/core/test/interaction-errors.test.ts
          pnpm exec eslint \
            packages/core/src/interaction-errors.ts \
            packages/core/test/interaction-errors.test.ts
          pnpm typecheck
          pnpm exec vitest run \
            packages/core/test/interaction-errors.test.ts \
            packages/server/test/typed-input-errors.test.ts
      - name: Commit product changes
        shell: bash
        run: |
          git add \
            packages/core/src/interaction-errors.ts \
            packages/core/test/interaction-errors.test.ts
          if git diff --cached --quiet; then
            echo "Interaction diagnostics are already refined."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Refine typed interaction diagnostics"
          git push origin HEAD:feat/typed-interaction-errors
