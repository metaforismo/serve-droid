---
"serve-droid": patch
---

Add host-authorized browser controls for starting and stopping bounded session recordings without exposing host path selection, while preserving `--record` auto-start behavior.
