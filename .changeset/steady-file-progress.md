---
"serve-droid": patch
---

Stream opt-in HTTP install and push phase events after file upload and emit standard MCP progress notifications for callers that request them, without inventing Android-side percentages.
