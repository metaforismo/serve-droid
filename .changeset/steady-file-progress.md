---
"serve-droid": patch
---

Stream opt-in HTTP install and push phase events after file upload, emit standard MCP progress notifications for callers that request them, and report CLI install/push phases on stderr while preserving the final stdout result, without inventing Android-side percentages.
