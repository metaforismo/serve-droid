---
"serve-droid": patch
---

Add evidence-based `DEVICE_LOCKED`, `SECURE_SCREEN`, and `INPUT_RESTRICTED` errors for failed Android interactions while preserving generic ADB and scrcpy failures when the available evidence is ambiguous.
