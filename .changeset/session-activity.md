---
"serve-droid": patch
---

Add a bounded, resumable, privacy-filtered session activity stream so authenticated clients can inspect recent structured device and control events even when recording is disabled.
