---
"serve-droid": patch
---

Add a live-session CLI client and `serve-droid activity` command for bounded privacy-filtered Activity snapshots, while reusing the same session-selection semantics for named tunnels.
