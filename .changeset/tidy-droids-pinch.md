---
"serve-droid": patch
---

Add synchronized two-finger gesture injection over the active scrcpy control channel, with aligned path validation, bounded moves, reverse release, fail-closed ADB behavior, and partial-failure cleanup.
