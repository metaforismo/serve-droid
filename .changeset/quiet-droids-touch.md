---
"serve-droid": patch
---

Route tap, swipe, multi-point gesture, mouse-wheel, and trackpad input through the active scrcpy control channel, with bounded serialization and ADB fallback only when scrcpy control is unavailable.
